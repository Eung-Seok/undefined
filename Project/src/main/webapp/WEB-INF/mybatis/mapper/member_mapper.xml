<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="MemberMapper">
    <insert id="insertMember" parameterType="com.app.dto.member.MemberDTO">
        INSERT INTO members (user_no, email, password, name, reg_date)
        VALUES (mem_seq.NEXTVAL, #{email}, #{password}, #{name}, SYSDATE)
    </insert>
</mapper>